# Generated by agentweld {{ agentweld_version }} — do not edit manually.
# Regenerate with: agentweld generate --only deploy_config
#
# Agent : {{ agent_name }}
#
# Run this agent:
#   docker compose up
#   curl http://localhost:{{ serve_port }}/.well-known/agent.json

services:
  {{ agent_slug }}:
    build: .
    ports:
      - "{{ serve_port }}:80"
    env_file: .env
    restart: unless-stopped
